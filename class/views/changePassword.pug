extends layouts/main

block content
    .from-container
        form(action=`videos${routes.changePassword}`, method="post")
            input(type="password", name="oldPassword", placeholder="Current Password", require=true)
            input(type="password", name="newPassword", placeholder="New Password", require=true)
            input(type="password", name="newPassword2", placeholder="Verify Password", require=true)
            input(type="submit", name="Change Password")
            